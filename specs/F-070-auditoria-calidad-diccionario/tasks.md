<!-- specs/F-070-auditoria-calidad-diccionario/tasks.md -->
# F-070 · Tareas

Rigor `estandar`: fase RED antes de cada implementacion, cobertura de lineas
cambiadas >= 80 % y campana de mutacion muestreada (20 mutantes, semilla fija)
sobre `etl_sigrid/domain/calidad_diccionario.py`. Una tarea = un commit
`F-070 Tn: ...`. Las cifras de censo son las de la version 16 (2026-09-08); si
al ejecutar hay otras, mandan las del dia y el informe las declara.

- [ ] T1: Escribir `docs/CALIDAD_DICCIONARIO.md`: los catorce criterios C1-C14 separados en lo que FALTA (C1-C10) y lo que SOBRA (C11-C14), la escala de tres gravedades medida por cuanto cambia una respuesta de la IA, la regla de corte de R3, los tres niveles de profundidad A/B/C con su justificacion, y quien revisa lo no automatizable y cuando  |  Verificacion: `pytest tests/test_f070_docs.py` (R1, R2, R3, R4, R5, R30)
- [ ] T2: FASE RED de lo que falta: `tests/test_f070_criterios.py` y `tests/test_f070_auditoria.py` con fixtures en memoria —una ficha buena y una por criterio incumplido— incluyendo una medida sumable sin `unidad` y una ficha sin el marcador `**NO contiene:**`  |  Verificacion: los dos ficheros en ROJO, con la traza pegada en el informe (R7, R10, R11, R12)
- [ ] T3: FASE RED de lo que sobra: `tests/test_f070_sobra.py` con el caso de la ventana de F-025 (cifra de conducta sin fecha y SQL cambiado despues), el de `compras.contratos` (ejemplo que la ficha no puede responder), dos copias divergentes de la misma afirmacion y dos objetos de consumo que responden lo mismo  |  Verificacion: fichero en ROJO con su traza (R15, R16, R17, R18, R19, R20)
- [ ] T4: Implementar `etl_sigrid/domain/calidad_diccionario.py` (`GRAVEDADES`, `NIVELES`, `CRITERIOS`, `Hallazgo`, `nivel_de`, `auditar`, `repeticiones`, `divergen`, `cifras_sin_fecha`, `presupuesto`, `viola_trinquete`) como funciones puras, salida ordenada y determinista, sin YAML, sin red y sin llamar a git  |  Verificacion: `pytest tests/test_f070_criterios.py tests/test_f070_auditoria.py tests/test_f070_sobra.py` en VERDE (R7-R21)
- [ ] T5: Implementar `etl_sigrid/infrastructure/diccionario/cambios_sql.py` (fecha del ultimo cambio por carpeta `sql/<capa>/` con `git log`), que degrada a vacio si git no responde  |  Verificacion: `pytest tests/test_f070_sobra.py -k cambios_sql`, incluido el caso sin git (R16)
- [ ] T6: Anadir el comando `auditar-diccionario` a `main.py` con `--esquema`, `--gravedad`, `--nivel` y `--solo-consumo`, salida que separa falta de sobra, presupuesto de contexto y `SystemExit(1)` solo con `grave`; nunca abre conexion  |  Verificacion: `pytest tests/test_f070_auditoria.py -k cli` y `python main.py auditar-diccionario --nivel A` sin conexion (R7, R8, R9, R13, R21)
- [ ] T7: Primera pasada mecanica sobre el censo entero y arranque de `progress/auditoria_F-070.md`: censo del dia, recuento por criterio y gravedad, presupuesto de contexto y la tabla con una fila por objeto y su nivel  |  Verificacion: `python main.py auditar-diccionario` y el informe con los 130 objetos listados (R6, R23)
- [ ] T8: Lote L1 — revision de nivel A de `compras` + `maestro` con las cuatro carencias del correo de Compras del 2026-09-05 como banco de pruebas, escribiendo el `SELECT` de cada `ejemplos_preguntas` (C12); cada hallazgo con su pregunta de negocio  |  Verificacion: hallazgos de L1 en el informe, cada `grave`/`serio` con pregunta escrita (R3, R18, R23)
- [ ] T9: Lote L2 — revision de nivel A de `retenciones` + `cierre` con el fallo de la obra 0694 del 2026-08-25 como banco de pruebas (distinguir `saldo_vivo`, `importe_vencido` y `neto_practicado`, y a que objeto va cada pregunta)  |  Verificacion: hallazgos de L2 en el informe (R23)
- [ ] T10: Lote L3 — `mart`, `stg`, `_meta` y `aux` (niveles A y B), con atencion a C11: las cifras de conducta heredadas de F-025, F-042 y F-052 y si su SQL ha cambiado despues  |  Verificacion: hallazgos de L3 en el informe (R15, R16, R17)
- [ ] T11: Lote L4 — las 56 fichas de `raw` a nivel C: que guarda, que NO es, a que objeto de consumo ir y sus columnas de union y filtro; y el puntero a `sigrid_tablas.md`, hoy repetido 56 veces, movido a la entrada del esquema  |  Verificacion: `python main.py auditar-diccionario --nivel C` sin hallazgos de C1, C5, C6 ni C13 pendientes fuera del trinquete (R5, R19)
- [ ] T12: Derivar en el informe los hallazgos que exceden la feature, citando F-054, F-046 o F-067, sin corregirlos aqui  |  Verificacion: `pytest tests/test_f070_docs.py -k derivaciones` sobre el informe (R22)
- [ ] T13: Corregir en `config/diccionario/*.yaml` TODOS los hallazgos `grave` de las dos mitades: las 28 medidas sumables sin `unidad`, los filtros obligatorios sin `WHERE`, las copias divergentes y las fichas que describen una conducta que el codigo ya no tiene  |  Verificacion: `python main.py auditar-diccionario` sin ningun `grave` (R25)
- [ ] T14: Escribir el limite del objeto (`**NO contiene:**`) en las 47 fichas de consumo y el `**Filtro obligatorio:**` con su `WHERE` donde una regla dura lo exige; lo que no quepa, al trinquete con fecha y dueno  |  Verificacion: `python main.py auditar-diccionario --gravedad serio` sin C4 ni C5 fuera del trinquete (R11)
- [ ] T15: Crear `config/calidad_pendientes.yaml` con los `serio` que quedan (objeto, criterio, motivo, fecha) y activar la puerta en la seccion 9 de `harness/init.sh` (censo entero, `grave`, trinquete), degradando con aviso si no hay Python  |  Verificacion: `pytest tests/test_f070_trinquete.py` y `bash harness/init.sh` (R28, R29)
- [ ] T16: Ampliar `preguntas_aceptacion` de `00_global.yaml` con las cuatro del correo de Compras (con `bloqueada_por: F-067` donde el dato no exista), la de la obra 0694 (control 34.523,22 EUR en 61 efectos) y la de vigencia de la ventana de F-025, cada una con su `respuesta_correcta` escrita ANTES de la pasada  |  Verificacion: `pytest tests/test_f070_bateria.py` (R24, R25)
- [ ] T17: Subir `version: 17` en `00_global.yaml` con su nota de cabecera y actualizar `docs/CONVENTIONS.md` y `CHECKPOINTS.md`  |  Verificacion: `pytest tests/test_f006_docs.py tests/test_f070_docs.py` (R25, R30)
- [ ] T18: Contrastar las fichas corregidas contra la base antes de publicar  |  Verificacion: MANUAL (humano) — `python main.py check-diccionario` y `python main.py check-unicidad` en verde, solo lectura, fuera de la ventana nocturna (C10)
- [ ] T19: Publicar la version 17 en `_meta`  |  Verificacion: MANUAL (humano) — `python main.py publicar-diccionario`; es una ESCRITURA contra Azure en produccion y la lanza el humano, nunca un agente (R25)
- [ ] T20: Crear `docs/PRUEBAS_MCP.md` y pasar la bateria completa contra el MCP real desde una sesion limpia SIN acceso a este repositorio, registrando fecha, version, veredicto por pregunta y causa de las que fallen  |  Verificacion: MANUAL (humano) — el registro con todas las preguntas juzgadas contra su `respuesta_correcta` (R26, R27)
- [ ] T21: Campana de mutacion muestreada sobre `etl_sigrid/domain/calidad_diccionario.py` y analisis por escrito de cada superviviente  |  Verificacion: `python -m harness.mutacion --feature F-070` y el analisis en el informe
- [ ] T22: Ejecutar `bash harness/init.sh` en verde  |  Verificacion: `bash harness/init.sh` termina con veredicto OK
