<!-- specs/F-110-fin-obra-cuatrimestral/tasks.md -->
# F-110 · Tareas

Rama `feature/F-110-fin-obra-cuatrimestral`. Rigor `critico`: fase RED
obligatoria, cobertura >= 80 % de lineas cambiadas y campana de mutacion ENTERA
con **0 supervivientes** (cada uno, test nuevo o justificacion aceptada por el
humano). Un commit por tarea (`F-110 Tn: ...`), en espanol. **Spec APROBADA por
el humano el 2026-09-25** con D1-D6 (`design.md` §Decisiones del humano): D5
conserva `ultimo_cierre` como columna INFORMATIVA.

- [x] T0: Comprobar que F-095 sigue `done` y que la aprobacion del 2026-09-25 (D1-D6) consta en `progress/current.md`; si no, `blocked` con el motivo.  |  Verificacion: `harness/features.json` y anotacion en `progress/impl_F-110.md`
- [x] T1: Escribir `tests/test_f110_fin_obra_cuatrimestral.py` en fase RED con un test por requisito R1-R23 (offline: texto de `05_fin_obra.sql` sin comentarios `--`, YAML del diccionario, cableado del step, orden topologico de `main.build_pipeline_steps`).  |  Verificacion: `pytest tests/test_f110_fin_obra_cuatrimestral.py` falla y la traza queda en `progress/impl_F-110.md`
- [x] T2: En la misma fase RED, fijar con test que plazo, `fuente_plazo`, constante 12, vencimiento, informativas (incluidos el CTE `cierres` y `ultimo_cierre`) y `terminada_sin_fin_obra` de `05_fin_obra.sql` quedan textualmente como en F-095 (R10-R13): son guardas de no regresion y pasan ya antes de tocar el SQL, mientras el resto de T1 esta en rojo.  |  Verificacion: `pytest tests/test_f110_fin_obra_cuatrimestral.py -k "r11 or r12 or r13"` en verde antes de tocar el SQL
- [x] T3: Reescribir a la regla nueva, citando F-110 en su docstring y sin borrar ninguno sin sustituto, los tests de F-095 que fijan la vieja: `test_f095_r19_fin_obra_y_fuente`, `test_f095_r20_sin_fecha_no_se_inventa` (mensaje), `test_f095_r21_guarda_cierre_vacio` (pasa a guarda de existencia, sin la de tabla vacia), `test_f095_r21_un_fallo_en_fin_obra_sale_con_su_nombre` (texto del error), `test_f095_d7_solo_fact_cierre`, `test_f095_r28_las_fichas_traen_las_cifras_medidas` (cifras de `fin_obra`), la entrada `fin_obra` de `CONTRATO_SQL` (el CTE `cierres` y `ci.ultimo_cierre` se quedan; cambia `fin` y se anaden `cuatrimestral`, `plan` y las dos columnas) y el docstring de cabecera del fichero. `test_f095_r18_ultimo_cierre_con_movimiento` NO se reescribe: la regla de `ultimo_cierre` no cambia (D5), solo su docstring dice que es informativa. Cualquier otro que caiga por la regla vieja se anade a esta lista en el informe.  |  Verificacion: `pytest tests/test_f095_retenciones_contables.py` falla SOLO en esos tests (traza en `progress/impl_F-110.md`)
- [x] T4: En `sql/retenciones/05_fin_obra.sql`, la guarda de R14 antes del `DROP`: cuatro `RAISE EXCEPTION 'fin_obra: ...'` (`mart.master_versiones_tipadas` inexistente, sin filas `Cuatrimestral`, `stg.plan_mensual` sin ambitos 8/11, y la de F-095 de `cierre.fact_cierre_mensual` inexistente, que se conserva); se retira la de tabla de cierre VACIA.  |  Verificacion: `test_f110_r14_guarda_*`, `test_f095_r21_*` reescritos
- [x] T5: En `05_fin_obra.sql`, conservar el CTE `cierres` y la columna `ultimo_cierre` tal cual (D5, informativa) y anadir los CTE `cuatrimestral` (D1: `MAX(version)` con `tipo_master = 'Cuatrimestral'`) y `plan` (D2: `MAX(anio_mes) FILTER (WHERE importe_mes <> 0)` en ambitos 8 y 11), unidos a `base` solo por `obra_id`, publicando `version_cuatrimestral` y `ultimo_mes_planificado` justo detras de `ultimo_cierre`.  |  Verificacion: `test_f110_r1_*` a `test_f110_r7_*`, `test_f110_r10_ultimo_cierre_informativo`, `test_f095_d7_*` reescrito (de `cierre` solo `fact_cierre_mensual`, de `mart` solo `master_versiones_tipadas`, de `stg` solo `plan_mensual`)
- [x] T6: En `05_fin_obra.sql`, `fecha_fin_obra` y `fuente_fin_obra` con el orden garantia -> ultimo dia del mes siguiente a `ultimo_mes_planificado` -> NULL (D3), valores `INICIO_GARANTIA` / `ULTIMO_CUATRIMESTRAL_MAS_1_MES`, sin `ELSE` y sin `ultimo_cierre` en el CTE `fin`; cabecera del fichero y `COMMENT ON TABLE` reescritos con la regla [H], el ejemplo literal del humano (cuatrimestral de junio 26 planificado hasta marzo 28 -> 30 de abril de 2028), D1-D6 y las medidas.  |  Verificacion: `test_f110_r5_ejemplo_del_humano`, `test_f110_r8_*`, `test_f110_r9_*`, `test_f110_r11_*`, `test_f110_r12_*`, `test_f095_r19_*` y `test_f095_contrato_expresion_a_expresion[fin_obra]` reescritos
- [x] T7: En `sql/retenciones/06_views_contables.sql`, solo el comentario de `SIN_FIN_OBRA` (R18), sin cambiar una linea de SQL.  |  Verificacion: `test_f110_r18_vista_sin_cambio_de_sql` (el `CASE` de `estado_vencimiento` y el `LEFT JOIN retenciones.fin_obra` literales) y `test_f095_r24_*` en verde
- [x] T8: En `build_retenciones_step.py`, docstring y comentario de `depends_on` (el fin de obra lee `mart.master_versiones_tipadas` y `stg.plan_mensual` de la misma noche; `cierre.fact_cierre_mensual`, de la anterior, solo para la columna informativa y sin fallar si esta vacia), sin tocar codigo, `SUB_PASOS`, `name`, `stage` ni `depends_on`.  |  Verificacion: `test_f110_r15_depends_on_intacto`, `test_f110_r17_*`, `pytest tests/test_f047_nocturna.py tests/test_f047_steps.py`
- [x] T9: Test de R16: en el orden topologico de la composicion real, `build_stg` y `build_mart` van antes que `build_retenciones`.  |  Verificacion: `test_f110_r16_orden_topologico`
- [x] T10: PROPAGACION 1/4 — `config/diccionario/retenciones.yaml`: ficha `fin_obra` (regla con el ejemplo literal del humano y decision del 2026-09-25; cifras antes/despues de `design.md` §Medidas, incluida la cola a cero de 15 obras; columnas nuevas; `ultimo_cierre` rotulada INFORMATIVA; valores de `fuente_fin_obra`; las de F-095 como historicas con fecha) y ficha `v_retencion_contable_obra` (valores y texto de `SIN_FIN_OBRA`).  |  Verificacion: `test_f110_r19_*`, `test_f095_r28_*` reescrito, puerta de diccionario de `bash harness/init.sh`
- [x] T11: PROPAGACION 2/4 — `config/diccionario/00_global.yaml`: `version` +1 y linea de historia (si F-108/F-109 ya subieron la version en `main`, la siguiente libre).  |  Verificacion: `test_f110_r20_version`, `pytest tests/test_f006_*.py`
- [x] T12: PROPAGACION 3/4 — `docs/ARCHITECTURE.md`: vineta de la retencion con la regla nueva, las dos lecturas de la misma noche y el ultimo cierre como informativo (con su noche de desfase).  |  Verificacion: revision del reviewer contra `design.md`
- [x] T13: PROPAGACION 4/4 — `azure-apps/datamart_seg_anual.md`: fila de `retenciones.fin_obra` y dependencias (dentro `mart.master_versiones_tipadas` y `stg.plan_mensual`; `cierre.fact_cierre_mensual` solo informativa y ya sin fallo por tabla vacia); commit en el repositorio `azure-apps`.  |  Verificacion: revision del reviewer (R23)
- [x] T14: Cerrar la fase VERDE: `pytest` completo en verde y cobertura >= 80 % de las lineas cambiadas.  |  Verificacion: `bash harness/init.sh`
- [x] T15: Campana de mutacion ENTERA del nivel `critico` con analisis de cada superviviente en `progress/mutacion_F-110.md` (0 supervivientes sin test o sin justificacion aceptada).  |  Verificacion: `python -m harness.mutacion --feature F-110`
- [ ] T16: Fotos ANTES: las consultas de `progress/spec_F-110.md` §Consultas reproducen `design.md` §Medidas (deriva del dia anotada).  |  Verificacion: MANUAL (humano) — por el MCP, solo lectura
- [ ] T17: `python main.py build-retenciones` y `python main.py apply-grants`, y los puntos DESPUES 1-5 del plan de `design.md` (922 filas; 198/36/688; `ultimo_cierre` ~304; viva por fuente; estados; caso `1-0686`; duracion del sub-paso `fin_obra`).  |  Verificacion: MANUAL (humano) en el entorno que el humano autorice
- [ ] T18: `python main.py check-declarados`, `check-unicidad`, `check-relaciones`, `check-diccionario`; despues `python main.py publicar-diccionario`, reinicio del MCP y despliegue de imagen nueva.  |  Verificacion: MANUAL (humano) — publicar y desplegar son escrituras contra Azure y las autoriza el humano
- [x] T19: Ejecutar `bash harness/init.sh` en verde.  |  Verificacion: `bash harness/init.sh`
