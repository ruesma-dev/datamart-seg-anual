<!-- specs/F-110-fin-obra-cuatrimestral/tasks.md -->
# F-110 · Tareas

Rama `feature/F-110-fin-obra-cuatrimestral`. Rigor `critico`: fase RED
obligatoria, cobertura >= 80 % de lineas cambiadas y campana de mutacion ENTERA
con **0 supervivientes** (cada uno, test nuevo o justificacion aceptada por el
humano). Un commit por tarea (`F-110 Tn: ...`), en espanol. Las tareas asumen las
opciones RECOMENDADAS de D1-D5 (`design.md`); si el humano elige otra, se
reescribe la tarea afectada antes de empezar.

- [ ] T0: Confirmar que el humano valido D1-D6 (anotado en `progress/current.md`) y que F-095 sigue `done`; si no, `blocked` con el motivo.  |  Verificacion: `harness/features.json` y anotacion en `progress/impl_F-110.md`
- [ ] T1: Escribir `tests/test_f110_fin_obra_cuatrimestral.py` en fase RED con un test por requisito R1-R23 (offline: texto de `05_fin_obra.sql` sin comentarios `--`, YAML del diccionario, cableado del step, orden topologico de `main.build_pipeline_steps`).  |  Verificacion: `pytest tests/test_f110_fin_obra_cuatrimestral.py` falla y la traza queda en `progress/impl_F-110.md`
- [ ] T2: En la misma fase RED, fijar con test que plazo, `fuente_plazo`, constante 12, vencimiento, informativas y `terminada_sin_fin_obra` de `05_fin_obra.sql` quedan textualmente como en F-095 (R11-R13): son guardas de no regresion y pasan ya antes de tocar el SQL, mientras el resto de T1 esta en rojo.  |  Verificacion: `pytest tests/test_f110_fin_obra_cuatrimestral.py -k "r11 or r12 or r13"` en verde antes de tocar el SQL
- [ ] T3: Reescribir a la regla nueva, citando F-110 en su docstring y sin borrar ninguno sin sustituto, los tests de F-095 que fijan la vieja: `test_f095_r18_ultimo_cierre_con_movimiento`, `test_f095_r19_fin_obra_y_fuente`, `test_f095_r20_sin_fecha_no_se_inventa` (mensaje), `test_f095_r21_guarda_cierre_vacio`, `test_f095_r21_un_fallo_en_fin_obra_sale_con_su_nombre` (texto del error), `test_f095_d7_solo_fact_cierre`, `test_f095_r28_las_fichas_traen_las_cifras_medidas` (cifras de `fin_obra`), la entrada `fin_obra` de `CONTRATO_SQL` y el docstring de cabecera del fichero; cualquier otro que caiga por la regla vieja se anade a esta lista en el informe.  |  Verificacion: `pytest tests/test_f095_retenciones_contables.py` falla SOLO en esos tests (traza en `progress/impl_F-110.md`)
- [ ] T4: En `sql/retenciones/05_fin_obra.sql`, sustituir la guarda de `cierre` por la de R14 (tres `RAISE EXCEPTION 'fin_obra: ...'`: tabla `mart.master_versiones_tipadas` inexistente, sin filas `Cuatrimestral`, `stg.plan_mensual` sin ambitos 8/11), antes del `DROP`.  |  Verificacion: `test_f110_r14_guarda_*`, `test_f095_r21_*` reescritos
- [ ] T5: En `05_fin_obra.sql`, retirar el CTE `cierres` y la columna `ultimo_cierre`, y anadir los CTE `cuatrimestral` (D1: `MAX(version)` con `tipo_master = 'Cuatrimestral'`) y `plan` (D2: `MAX(anio_mes) FILTER (WHERE importe_mes <> 0)` en ambitos 8 y 11), unidos a `base` solo por `obra_id`, y publicar `version_cuatrimestral` y `ultimo_mes_planificado` donde estaba `ultimo_cierre`.  |  Verificacion: `test_f110_r1_*` a `test_f110_r7_*`, `test_f110_r10_sin_cierre`, `test_f095_d7_*` reescrito
- [ ] T6: En `05_fin_obra.sql`, `fecha_fin_obra` y `fuente_fin_obra` con el orden garantia -> ultimo dia del mes siguiente a `ultimo_mes_planificado` -> NULL (D3), valores `INICIO_GARANTIA` / `ULTIMO_CUATRIMESTRAL_MAS_1_MES`, sin `ELSE`; cabecera del fichero y `COMMENT ON TABLE` reescritos con la regla [H], D1-D5 y las medidas.  |  Verificacion: `test_f110_r8_*`, `test_f110_r9_*`, `test_f110_r11_*`, `test_f095_r19_*` y `test_f095_contrato_expresion_a_expresion[fin_obra]` reescritos
- [ ] T7: En `sql/retenciones/06_views_contables.sql`, solo el comentario de `SIN_FIN_OBRA` (R18), sin cambiar una linea de SQL.  |  Verificacion: `test_f110_r18_vista_sin_cambio_de_sql` (el `CASE` de `estado_vencimiento` y el `LEFT JOIN retenciones.fin_obra` literales) y `test_f095_r24_*` en verde
- [ ] T8: En `build_retenciones_step.py`, docstring y comentario de `depends_on` (lee `mart.master_versiones_tipadas` y `stg.plan_mensual` de la misma noche; ya no `cierre`), sin tocar codigo, `SUB_PASOS`, `name`, `stage` ni `depends_on`.  |  Verificacion: `test_f110_r15_depends_on_intacto`, `test_f110_r17_*`, `pytest tests/test_f047_nocturna.py tests/test_f047_steps.py`
- [ ] T9: Test de R16: en el orden topologico de la composicion real, `build_stg` y `build_mart` van antes que `build_retenciones`.  |  Verificacion: `test_f110_r16_orden_topologico`
- [ ] T10: PROPAGACION 1/4 — `config/diccionario/retenciones.yaml`: ficha `fin_obra` (regla y decision del 2026-09-25, cifras antes/despues de `design.md` §Medidas, columnas nuevas, fuera `ultimo_cierre`, valores de `fuente_fin_obra`; las de F-095 como historicas con fecha) y ficha `v_retencion_contable_obra` (valores y texto de `SIN_FIN_OBRA`).  |  Verificacion: `test_f110_r19_*`, `test_f095_r28_*` reescrito, puerta de diccionario de `bash harness/init.sh`
- [ ] T11: PROPAGACION 2/4 — `config/diccionario/00_global.yaml`: `version` +1 y linea de historia (si F-108/F-109 ya subieron la version en `main`, la siguiente libre).  |  Verificacion: `test_f110_r20_version`, `pytest tests/test_f006_*.py`
- [ ] T12: PROPAGACION 3/4 — `docs/ARCHITECTURE.md`: vineta de la retencion con la regla nueva, las dos lecturas y el fin del desfase de una noche.  |  Verificacion: revision del reviewer contra `design.md`
- [ ] T13: PROPAGACION 4/4 — `azure-apps/datamart_seg_anual.md`: fila de `retenciones.fin_obra` y dependencias (fuera `cierre.fact_cierre_mensual`, dentro `mart.master_versiones_tipadas` y `stg.plan_mensual`); commit en el repositorio `azure-apps`.  |  Verificacion: revision del reviewer (R23)
- [ ] T14: Cerrar la fase VERDE: `pytest` completo en verde y cobertura >= 80 % de las lineas cambiadas.  |  Verificacion: `bash harness/init.sh`
- [ ] T15: Campana de mutacion ENTERA del nivel `critico` con analisis de cada superviviente en `progress/mutacion_F-110.md` (0 supervivientes sin test o sin justificacion aceptada).  |  Verificacion: `python -m harness.mutacion --feature F-110`
- [ ] T16: Fotos ANTES: las consultas de `progress/spec_F-110.md` §Consultas reproducen `design.md` §Medidas (deriva del dia anotada).  |  Verificacion: MANUAL (humano) — por el MCP, solo lectura
- [ ] T17: `python main.py build-retenciones` y `python main.py apply-grants`, y los puntos DESPUES 1-5 del plan de `design.md` (922 filas; 198/36/688; viva por fuente; estados; caso `1-0686`; duracion del sub-paso `fin_obra`).  |  Verificacion: MANUAL (humano) en el entorno que el humano autorice
- [ ] T18: `python main.py check-declarados`, `check-unicidad`, `check-relaciones`, `check-diccionario`; despues `python main.py publicar-diccionario`, reinicio del MCP y despliegue de imagen nueva.  |  Verificacion: MANUAL (humano) — publicar y desplegar son escrituras contra Azure y las autoriza el humano
- [ ] T19: Ejecutar `bash harness/init.sh` en verde.  |  Verificacion: `bash harness/init.sh`
