<!-- specs/F-073-tablas-nuevas-y-enriquecimiento/tasks.md -->
# F-073 · Tareas

Rigor `estandar`: **fase RED obligatoria** en los requisitos centrales (R1–R5,
R7–R18, R21–R23) con la traza real pegada en `progress/impl_F-073.md`, más
cobertura y campaña de mutación. Rama `feature/F-073-tablas-nuevas-y-enriquecimiento`.
Un commit por tarea (`F-073 Tn: ...`). `git add` de ficheros concretos, nunca `-A`.

- [x] T1: Medir en SOLO LECTURA contra la base, con el cliente del ETL (no el MCP: `raw` no es consultable), los nombres exactos de columna de `raw.conest` (tipo, código de estado, nombre), `raw.auxefp` (el nombre del medio: `tables_sigrid.yaml` dice `est` y el informe de bloque dice `res`, se contradicen) y los ocho campos de dirección de `raw.obr`; anotarlos en `progress/impl_F-073.md`  |  Verificación: MANUAL (humano) — la tabla de nombres medidos consta en el informe antes de existir ningún SQL nuevo
- [x] T2: Medir y anotar el porcentaje informado de cada columna nueva (dir1, dir2, dircpo, dir, munide, proide, estado) sobre las 921 fichas de `maestro.obras`, y el recuento de `raw.cen` (804), del puente (683), de `raw.conest` (193) y de `raw.auxpag` (69)  |  Verificación: MANUAL (humano) — cifras en `progress/impl_F-073.md`, base de las fichas de R6, R12 y R15
- [x] T3: Escribir `tests/test_f073_sql.py` con los asserts de texto de R2, R3, R4, R5 sobre `sql/maestro/04_centros_coste.sql`  |  Verificación: `pytest tests/test_f073_sql.py` en ROJO (el fichero SQL no existe); traza pegada en el informe
- [x] T4: Crear `sql/maestro/04_centros_coste.sql` con la vista `maestro.centros_coste` (R1–R5): `LEFT JOIN LATERAL` con `ORDER BY` y `LIMIT 1`, `JOIN raw.obr` dentro del lateral, sin `cen.obride` ni aritmética sobre el `ide`  |  Verificación: `pytest tests/test_f073_sql.py` en verde
- [x] T5: Ampliar `tests/test_f073_sql.py` con los asserts de R19 y R20 sobre `sql/maestro/05_estados_documento.sql` (las 193 filas, sin filtro de tipo)  |  Verificación: `pytest tests/test_f073_sql.py` en ROJO; traza pegada
- [x] T6: Crear `sql/maestro/05_estados_documento.sql` con la vista `maestro.estados_documento`, usando los nombres de columna medidos en T1  |  Verificación: `pytest tests/test_f073_sql.py` en verde
- [ ] T7: Ampliar `tests/test_f073_sql.py` con los asserts de R7, R8, R9, R10, R13, R14, R16, R17 y R18 sobre `sql/maestro/01_obras.sql` (incluido: ninguna columna actual desaparece del `SELECT`)  |  Verificación: `pytest tests/test_f073_sql.py` en ROJO; traza pegada
- [ ] T8: Modificar `sql/maestro/01_obras.sql`: dirección con el vocabulario de `maestro.proveedores`, `municipio`/`municipio_id` y `provincia`/`provincia_id`, las dos marcas por `EXISTS` sobre `stg`, `estado` desde `raw.conest` con el tipo de obra y guarda anti-multiplicación, y corregir la cabecera que hoy afirma que las obras no tienen dirección  |  Verificación: `pytest tests/test_f073_sql.py` en verde
- [ ] T9: Escribir los asserts de R21 y R22 sobre `sql/compras/04_formas_pago.sql` (`plazo_formula` verbatim, sin `dias_pago` numérico) y el de R23 (`01_documentos.sql` no cambia)  |  Verificación: `pytest tests/test_f073_sql.py` en ROJO; traza pegada
- [ ] T10: Crear `sql/compras/04_formas_pago.sql` con la vista `compras.formas_pago` (`raw.auxpag` + `raw.auxefp`)  |  Verificación: `pytest tests/test_f073_sql.py` en verde
- [ ] T11: Escribir `tests/test_f073_pipeline.py`: `build_maestros` declara los sub-pasos `centros_coste` y `estados_documento` en el orden de sus ficheros, `build_compras` declara `formas_pago`, y `BuildMaestrosStep.depends_on` incluye `build_stg` (R27)  |  Verificación: `pytest tests/test_f073_pipeline.py` en ROJO; traza pegada
- [ ] T12: Modificar `build_maestros_step.py` (dos sub-pasos, `depends_on`, docstring corregido: ya no es raw-only y sí forma parte de `run-all`) y `build_compras_step.py` (un sub-paso)  |  Verificación: `pytest tests/test_f073_pipeline.py` en verde
- [ ] T13: Escribir `tests/test_f073_diccionario.py`: existe ficha para los tres objetos nuevos y para cada columna nueva de `maestro.obras`; la de la dirección declara porcentaje informado (R12) y la de `tiene_seguimiento` la diferencia con `mart` (R15); `00_global.yaml` en versión 19 (R28)  |  Verificación: `pytest tests/test_f073_diccionario.py` en ROJO; traza pegada
- [ ] T14: Escribir las fichas en `config/diccionario/maestro.yaml` (`centros_coste`, `estados_documento` y las columnas nuevas de `obras`) y en `compras.yaml` (`formas_pago`), con grano, significado y trampas: R6 (683/804 y que `cen.obride` no sirve), R11 y R12 (dos de cada tres obras sin dirección), R15, R20 y R22  |  Verificación: `pytest tests/test_f073_diccionario.py` en verde
- [ ] T15: Subir `version` a 19 en `config/diccionario/00_global.yaml` con su nota de qué cambia  |  Verificación: `pytest tests/test_f006_publicacion.py tests/test_f073_diccionario.py` en verde
- [ ] T16: Ejecutar la suite completa y la puerta de cobertura de las líneas cambiadas  |  Verificación: `bash harness/init.sh` en verde
- [ ] T17: Campaña de mutación y análisis de supervivientes  |  Verificación: `python -m harness.mutacion --feature F-073`; si genera 0 mutantes, campaña MANUAL con la tabla fila a fila (fichero, línea, texto exacto original → mutado, resultado y nº de fallos) que exige `CHECKPOINTS.md` C4 bis
- [ ] T18: Dejar en `progress/current.md` las verificaciones MANUAL con su comando exacto: recuento de `maestro.centros_coste` (804 filas, 683 con obra, `centro_coste_id` único), `maestro.estados_documento` (193), `compras.formas_pago` (69), `maestro.obras` en **921 filas** y sin columnas perdidas, `python main.py check-declarados` y `python main.py check-diccionario` en verde  |  Verificación: MANUAL (humano) — las ejecuta el humano contra Azure; ningún agente escribe
- [ ] T19: Dejar anotada, también como MANUAL, la prueba de que el puente resuelve F-045: los 261 valores de `retenciones.movimientos.obra_id` casan 261/261 contra `maestro.centros_coste.centro_coste_id`, **sin tocar `sql/retenciones/**`**  |  Verificación: MANUAL (humano) — consulta de solo lectura, resultado anotado en `progress/current.md`
- [ ] T20: Escribir `progress/impl_F-073.md` (≤ 220 líneas) con las trazas RED, las mediciones de T1 y T2, y la sección «Evidencias» con los cuatro números y el nº de workers de la campaña  |  Verificación: `python -m harness.tamano --feature F-073` en verde
- [ ] T21: Ejecutar `bash harness/init.sh` en verde  |  Verificación: `bash harness/init.sh` termina con exit code 0
