<!-- specs/F-109-partidas-codigo-no-unico/tasks.md -->
# F-109 · Tareas

Rama `feature/F-109-partidas-codigo-no-unico` (desde `main`). Rigor `estandar`: fase RED, cobertura de lo cambiado y campaña de mutación muestreada (`python -m harness.mutacion --feature F-109 --base main`). Un commit por tarea (`F-109 Tn: ...`), en español, sin `git add -A`. **Ningún SQL se toca** (`design.md` §3). Las decisiones D1-D5 de `design.md` §8 las valida el humano ANTES de T1; sin D2 se omiten R9-R11 y T4; con D1 (b) se añade una tarea de declarar la clave alternativa y F-109 espera a F-108.

- [x] T1: Escribir `tests/test_f109_partidas_codigo.py` en fase RED, un test por requisito R1-R17 (offline, sobre el diccionario real con `cargar_diccionario` y `derivar_avisos`, y un `tmp_path` para el caso sintético de R15), según `design.md` §6.  |  Verificación: `python -m pytest tests/test_f109_partidas_codigo.py -q` falla en todo salvo R15 (guarda que ya se cumple) y la traza queda en `progress/impl_F-109.md`
- [x] T2: `stg.yaml`: fichas de `stg.partidas` (`descripcion`, `obra_id`, `codigo_partida`, `ruta_capitulos`) con el texto de `design.md` §4 (R1, R5, R6, R7).  |  Verificación: `test_f109_r1`, `r4`-`r7`, `pytest tests/test_f006_fichas.py tests/test_f006_stg_trampas.py tests/test_f052_huella_dimension.py`
- [ ] T3: `mart.yaml`, `compras.yaml` y `cierre.yaml`: `fact_seguimiento_mensual.codigo_partida`, `v_pbi_dim_partida` (`obra_id`, `codigo_partida`, `partida_label`), `v_pbi_dim_partida_niveles.nivel_1..6`, `v_pbi_partida_coste.codigo_partida` y `v_pbi_dim_subcategoria_ci` (R2, R3, R8, R13, R14).  |  Verificación: `test_f109_r2`, `r3`, `r4`, `r8`, `r13`, `r14`, `pytest tests -k "f006 or f102"`
- [ ] T4: `00_global.yaml`: regla `R-PARTIDA-CODIGO-NO-UNICO` tras `R-LINEA-ID-NO-UNICA`, con el texto de `design.md` §5 (R9-R12).  |  Verificación: `test_f109_r9`-`r12`, `pytest tests/test_f006_reglas.py tests/test_f006_publicacion.py`
- [ ] T5: `00_global.yaml`: `version` a la siguiente de `main` (33 hoy, D4) con su comentario de historia en cabeza (R16); revisar tests que claven la versión exacta y pasarlos a `>=` si los hay.  |  Verificación: `test_f109_r16`, `pytest tests -k "version or publicacion"`
- [ ] T6: `test_f109_r15`: el trinquete de ficheros que agrupan por `(obra_id, codigo_partida)` en `sql/**` (`design.md` §7.2), con el caso sintético que demuestra que muerde.  |  Verificación: `test_f109_r15` pasa y, con un tercer fichero sintético, falla nombrándolo
- [ ] T7: `docs/ARCHITECTURE.md`: entrada «El código de partida no es único ni dentro de su obra (F-109)» tras la de F-052, con las tres causas y la orden de unir por `partida_id` (R17).  |  Verificación: `test_f109_r17`
- [ ] T8: Campaña de mutación muestreada y cobertura; análisis en `progress/impl_F-109.md` (tope 220 líneas), diciendo explícitamente si no hay Python de producción que mutar.  |  Verificación: `python -m harness.mutacion --feature F-109 --base main` escribe `progress/mutacion_F-109.md`
- [ ] T9: MANUAL (humano, solo lectura, con su `.env` o el MCP): la consulta de `design.md` §7.1 (esperado ~5.200 pares); después `python main.py publicar-diccionario` (versión de T5), escritura contra Azure que autoriza el humano, y reinicio de `mcp-bbdd`; comprobar en `_meta.diccionario_reglas` la regla nueva y en `_meta.v_diccionario` las tres fichas (R18).  |  Verificación: MANUAL (humano), salida pegada en `progress/current.md`
- [ ] T10: Si D3 (a) se aprueba, dejar redactada en `progress/impl_F-109.md` la ficha de la feature nueva «nombres de escalón por ancestro, no por código» para que el líder la dé de alta (no se toca `features.json` desde el implementer).  |  Verificación: la sección existe en el informe
- [ ] T11: Ejecutar `bash harness/init.sh` en verde.  |  Verificación: `bash harness/init.sh` termina con ENTORNO LISTO
